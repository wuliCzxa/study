	ORG		0
	LJMP	MAIN
	ORG		50H

	MAIN:
		MOV R0,	#40H
		MOV	R2,	#10H
	LOOP:
		MOV	A,	R0
		CLR	C
		SUBB	A,	#40H
		MOV	@R0,	A
		INC	R0
		DJNZ	R2,	LOOP

		MOV	R0,	#40H
		MOV	DPTR,	#1050H
		MOV	R2,	#10H
	COPY_LOOP:
		MOV	A,	@R0
		MOVX	@DPTR,	A
		INC	R0
		INC	DPTR
		DJNZ	R2,	COPY_LOOP

		SJMP	$
	END